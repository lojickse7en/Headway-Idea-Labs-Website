Quiet secondary CTA. Use when a section already carries a gradient Button and a second action would compete.

```jsx
<LinkArrow href="#contact">Book a Discovery Call</LinkArrow>
```

The arrow slides right on hover (gap grows 9px → 14px). Orange text, no underline.
